export interface MovieCard {
    id: number;
    title: string;
    posterUrl: string;
    releaseDate: Date;
}

// vs code
// cmd + shift + p => open all the setting in vs Code
// cmd + p => serch
// alt + shift + F => Formating your code
// alt + shift + Arrow down => pastes currently line below
// Alt + O