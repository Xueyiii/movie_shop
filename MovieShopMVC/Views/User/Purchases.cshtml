@model ApplicationCore.Models.PurchaseResponseModel;

@{
    ViewData["Title"] = "Purchases";
    // ViewBag.Title = "title";
    // Layout = "_Layout";
}
<form asp-controller="User" asp-action="Purchases">
    <h2>Purchase</h2>
    <div class="rounded">
        <div class="container-fluid">
            <div class="row">
                @foreach (var movie in Model)
                {
                    <div class="col-xl-2 col-sm-4 col-lg-3">
                        <partial name="_MovieCard" model="movie"/>
                    </div>
                }
            </div>
        </div>
    </div>
    }
</form>
